import { JSX } from "preact";
import { UploadedFile, UploadInterface } from "upload-js";
import { UploaderOptionsRequired } from "../../../../../UploaderOptions";
interface Props {
    imageCount: number;
    imageIndex: number;
    onImageEdited: (editedFile: UploadedFile | undefined) => void;
    options: UploaderOptionsRequired;
    originalImage: UploadedFile;
    upload: UploadInterface;
}
export declare const ImageEditor: ({ imageCount, imageIndex, originalImage, upload, onImageEdited, options }: Props) => JSX.Element;
export {};
